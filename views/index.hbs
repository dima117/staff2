<!DOCTYPE html>
<html>

<head>
    <meta charset="utf-8" />
    {{{title}}}
    {{{meta}}}
</head>

<body {{{bodyAttributes}}}>
    <div id="root">{{{html}}}</div>
    {{#each assets.js}}
    <script src="/{{{this}}}"></script>
    {{/each}}
</body>

</html>